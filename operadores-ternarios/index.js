const pontuacao = 1000
const nivel = pontuacao >= 1000 ? 'Usuário VIP': 'Usuário Normal'
console.log(nivel)

/* Método normal

if (pontuação >= 1000) {
    console.log(`Usuário VIP`)
}else {
    console.log(`suário Normal`)
} 
*/